{%- capture config_json -%}
{
  "heading": {{ section.settings.heading | json }},
  "subtext": {{ section.settings.subtext | json }},
  "form_errors": {% render 'customer-form-errors' %},
  "form_type": {{ form.type | json }}
}
{%- endcapture -%}

{% render 'react-mount',
  mount_id: 'customer-activate',
  component: 'CustomerActivate',
  config_json: config_json,
  shopify_attributes: section.shopify_attributes
%}

{% schema %}
{
  "name": "Customer activate account",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Activate account" },
    {
      "type": "text",
      "id": "subtext",
      "label": "Subtext",
      "default": "Create a password to activate your account."
    }
  ]
}
{% endschema %}
